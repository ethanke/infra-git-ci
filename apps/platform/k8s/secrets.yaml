# Encrypted with SOPS - placeholder for secrets
# Run: sops -e -i secrets.yaml to encrypt
apiVersion: v1
kind: Secret
metadata:
  name: platform-secrets
  namespace: platform
type: Opaque
stringData:
  database-url: "postgresql://user:password@postgres.db.svc.cluster.local:5432/lum_platform"
  session-secret: "your-session-secret-here"
  firebase-api-key: "your-firebase-api-key"
  firebase-auth-domain: "your-project.firebaseapp.com"
  firebase-project-id: "your-project-id"
  stripe-secret-key: "sk_test_xxx"
  stripe-publishable-key: "pk_test_xxx"
