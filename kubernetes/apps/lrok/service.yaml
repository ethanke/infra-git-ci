apiVersion: v1
kind: Service
metadata:
  name: lrok-service
  namespace: lrok
  labels:
    app: lrok
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: lrok
