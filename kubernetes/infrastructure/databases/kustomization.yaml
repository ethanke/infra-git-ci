# Databases Infrastructure
# PostgreSQL and Redis for lum.tools services

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production-db

resources:
  - namespace.yaml
  - postgres/
  - redis/
