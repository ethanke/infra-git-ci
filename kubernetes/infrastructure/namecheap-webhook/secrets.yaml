# SOPS-encrypted credentials for Namecheap API
# IMPORTANT: Encrypt with SOPS before committing!
# sops -e secrets.yaml > secrets.enc.yaml && rm secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: namecheap-credentials
  namespace: external-dns
type: Opaque
stringData:
  # Namecheap API credentials
  api-key: "e0d30246a2ed4d56ba3d1b3cab29caa2"
  api-user: "ethanke"
  # Client IP must be whitelisted in Namecheap console
